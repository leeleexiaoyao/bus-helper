.tag-editor-page {
  min-height: 100vh;
  padding: 24rpx 32rpx calc(env(safe-area-inset-bottom) + 132rpx);
  box-sizing: border-box;
  background: #f4f4f3;
}

picker {
  display: block;
}

.tag-editor-content {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

.editor-section-title {
  margin-bottom: 16rpx;
  color: #111111;
  font-size: 28rpx;
  font-weight: 600;
  line-height: 1.4;
}

.editor-card {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  border-radius: 0;
  background: transparent;
  overflow: visible;
}

.editor-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 128rpx;
  padding: 0 32rpx;
  border-radius: 24rpx;
  background: #ffffff;
  box-sizing: border-box;
}

.editor-row--avatar {
  min-height: 144rpx;
}

.editor-row-label {
  color: #111111;
  font-size: 28rpx;
  font-weight: 400;
  line-height: 1.4;
}

.editor-row-main {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10rpx;
  flex: 1;
  min-width: 0;
  margin-left: 24rpx;
}

.editor-row-main--avatar {
  gap: 12rpx;
}

.editor-row-value-wrap {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex: 1;
  min-width: 0;
}

.editor-row-value-wrap--lifted {
  transform: translateY(-12rpx);
}

.editor-row-value {
  max-width: 100%;
  color: #111111;
  font-size: 26rpx;
  line-height: 1.4;
  text-align: right;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.editor-row-value--muted,
.editor-row-value--placeholder {
  color: #c2c2c2;
}

.editor-row-caret {
  width: 12rpx;
  height: 12rpx;
  border-top: 2rpx solid rgba(17, 17, 17, 0.36);
  border-right: 2rpx solid rgba(17, 17, 17, 0.36);
  transform: rotate(45deg);
  flex-shrink: 0;
}

.editor-divider {
  display: none;
}

.editor-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: #e5e5e5;
}

.editor-avatar--fallback {
  display: block;
}

.editor-persona-thumb {
  width: 60rpx;
  height: 60rpx;
  border-radius: 18rpx;
  background: #f6f6f6;
  flex-shrink: 0;
}

.editor-textarea {
  width: 100%;
  min-height: 144rpx;
  padding: 28rpx 32rpx;
  border-radius: 24rpx;
  background: #ffffff;
  box-sizing: border-box;
  color: #111111;
  font-size: 26rpx;
  line-height: 1.6;
}

.editor-tag-list {
  margin-top: 18rpx;
  gap: 12rpx;
}

.editor-tag-chip {
  padding: 10rpx 18rpx;
  background: #ffffff;
  color: #5e6b7a;
  font-size: 22rpx;
}

.editor-save-bar {
  position: fixed;
  left: 32rpx;
  right: 32rpx;
  bottom: calc(env(safe-area-inset-bottom) + 24rpx);
  z-index: 10;
}

.editor-save-btn {
  width: 100%;
  height: 92rpx;
  margin: 0;
  padding: 0;
  border-radius: 999rpx;
  background: #d8f940;
  color: #111111;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  font-weight: 500;
  box-sizing: border-box;
}

.editor-save-btn::after {
  border: none;
}

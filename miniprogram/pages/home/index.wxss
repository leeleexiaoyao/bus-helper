.home-page {
  min-height: 100vh;
  position: relative;
  padding-top: 40rpx;
  padding-right: 0;
  padding-left: 0;
  padding-bottom: 40rpx;
  background: linear-gradient(180deg, #d7deea 0%, #fafafa 100%);
}

auth-gate {
  display: block;
  margin: 0 20rpx;
  position: relative;
  z-index: 1;
}

.home-hero {
  padding: 0 20rpx;
  position: relative;
  z-index: 1;
}

.viewer-row {
  display: flex;
  align-items: center;
  gap: 18rpx;
  margin-top: 8rpx;
  padding-left: 2rpx;
}

.viewer-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  flex-shrink: 0;
  border: 3rpx solid rgba(255, 255, 255, 0.72);
}

.viewer-avatar--fallback {
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #d78871 0%, #dd9d86 100%);
  color: #fff;
  font-size: 30rpx;
  font-weight: 700;
}

.viewer-copy {
  flex: 1;
}

.viewer-name {
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1.2;
}

.viewer-seat {
  margin-top: 8rpx;
  color: #c6c8cb;
  font-size: 20rpx;
}

.trip-panel {
  margin-top: 42rpx;
  margin-left: 20rpx;
  margin-right: 20rpx;
  margin-bottom: 24rpx;
  padding: 16rpx 16rpx 44rpx;
  min-height: calc(100vh - 388rpx);
  box-sizing: border-box;
  background: #ffffff;
  border-radius: 40rpx;
  overflow: hidden;
  position: relative;
  z-index: 1;
}

.trip-tabs {
  display: flex;
  justify-content: space-between;
  gap: 16rpx;
  align-items: center;
  padding: 0 16rpx 8rpx;
}

.tabs-left {
  display: flex;
  gap: 46rpx;
  align-items: center;
}

.home-tab {
  position: relative;
  padding-bottom: 8rpx;
  color: var(--text-primary);
  opacity: 0.4;
  font-size: 28rpx;
  font-weight: 400;
}

.home-tab.is-active {
  color: var(--text-primary);
  opacity: 1;
  font-weight: 700;
}

.tabs-stat {
  color: #c6c8cb;
  font-size: 28rpx;
}

.entry-card {
  margin: 0 20rpx;
  padding: 34rpx 28rpx;
  position: relative;
  z-index: 1;
}

.entry-actions {
  margin-top: 28rpx;
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}
